import { AppShell } from '../../features/navigation/app-shell';
import { ReelsPage } from '../../features/reels/reels-page';

export default function Page() {
  return (
    <AppShell>
      <ReelsPage />
    </AppShell>
  );
}
